platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven

inputs:
- name: demo1Spring

outputs:
- name: built-project

run:
  path: sh
  args:
  - -exc
  - |
    cd demo1Spring
    mvn package
    cp ./target/demo1Spring-SNAPSHOT.jar ../built-project/demo.jar
