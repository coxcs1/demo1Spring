platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven

inputs:
- name: demo1Spring

outputs:
- name: built-project

run:
  path: sh
  args:
  - -exc
  - |
    cd demo1
    mvn package
    cp ./target/demo1-0.0.1-SNAPSHOT.jar ../built-project/demo.jar
